# Библиотеки

Для запуска проекта поместите сюда файл:

- `jade.jar`

В методических указаниях JADE подключается как локальная библиотека. В этом проекте используется тот же подход: Gradle ожидает файл `libs/jade.jar`.
